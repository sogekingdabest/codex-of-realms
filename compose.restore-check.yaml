services:
  mailpit:
    ports: !reset []
  postgres:
    ports: !reset []

  keycloak:
    ports: !reset []

  app:
    ports: !reset []

  web:
    ports: !reset []

  ollama:
    ports: !reset []

  source-verifier:
    image: postgres:18.1-trixie
    command: ["sh", "-c", "test -d /restore/sources"]
    volumes:
      - source-data:/restore/sources
