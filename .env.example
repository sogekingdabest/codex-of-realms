# Copy this file to .env and replace both values before starting Docker Compose.
# The values below are intentionally empty; real credentials must never be committed.
POSTGRES_PASSWORD=
KEYCLOAK_ADMIN_PASSWORD=
